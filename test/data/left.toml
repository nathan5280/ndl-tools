[tool.poetry]
name = "ndlt"
version = "0.1.0"
description = "Tools to sort and compare nested dicts and lists."
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
ydiff = "^1.1"

[tool.poetry.dev-dependencies]
ipython = "^7.16.1"
pytest = "^5.4.3"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
